# mac-app

The native SwiftUI macOS app lives here: reads the serial stream from the Arduino, renders it as a live scrolling multi-channel strip chart, and (later) handles session recording and event markers.

Nothing here yet — see the repo root README's roadmap for the current build stage.
